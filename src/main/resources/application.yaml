spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/springdatajpa
    username: msbeigi
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        formar-sql: true
